export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {const p=await searchParams;return <div className="login" style={{marginLeft:'-240px'}}><div className="login-card"><div className="eyebrow">Job Agent</div><h1>Private dashboard</h1><p className="sub">Enter the password configured in Vercel.</p>{p.error?<div className="blocker">Incorrect password.</div>:null}<form action="/api/auth/login" method="post" className="grid"><input className="input" type="password" name="password" autoComplete="current-password" placeholder="Dashboard password" required/><button className="btn primary" type="submit">Sign in</button></form></div></div>}
